<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Steam Game Search</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      color: #fff;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: Arial, sans-serif;
    }
    .search-box {
      background: rgba(20, 20, 20, 0.9);
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(0, 255, 200, 0.4);
      text-align: center;
      width: 400px;
    }
    .btn-search {
      background: linear-gradient(45deg, #00ffcc, #0099ff);
      border: none;
      color: #000;
      font-weight: bold;
      border-radius: 25px;
      padding: 10px 20px;
      transition: all 0.3s;
    }
    .btn-search:hover {
      box-shadow: 0 0 20px rgba(0, 255, 200, 0.8);
      transform: translateY(-2px);
    }
  </style>
</head>
<body>
  <div class="search-box">
    <h2>🎮 Steam Oyun Ara</h2>
    <p>Bir oyun AppID gir ve detaylara bak.</p>
    <form action="/search" method="POST">
      <input type="text" name="appId" class="form-control mb-3" placeholder="Örn: 730 (CS2)">
      <button type="submit" class="btn btn-search w-100">Ara</button>
    </form>
  </div>
</body>
</html>
